import 'package:flutter/material.dart';

enum EditMode { ADD, EDIT }

const NOTESHEADERSTYLE =
    const TextStyle(fontSize: 27, fontWeight: FontWeight.bold);

const NOTECARDTITLESTYLE =
    const TextStyle(color: Colors.black, fontSize: 18.0, height: 1.4);
